import React from 'react';

const Blog = () => {
    return (
        <div>
            when to use context api
        what is custom hook
        what is useRef
        what is useMemo
        </div>
    );
};

export default Blog;